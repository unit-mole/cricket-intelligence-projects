[build-system]
requires=["setuptools>=75"]
build-backend="setuptools.build_meta"

[project]
name="icc-world-cup-intelligence-production"
version="1.0.0"
requires-python=">=3.12"

[tool.setuptools.packages.find]
where=["src"]
